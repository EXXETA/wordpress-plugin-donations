{
  "name": "wordpress-donations-plugin",
  "version": "1.0.0",
  "description": "JS project setup for wordpress plugin",
  "main": "build/index.js",
  "scripts": {
    "start": "wp-scripts start",
    "build": "npm run styles ; wp-scripts build",
    "styles": "node-sass --output-style compressed styles/banner.scss styles/banner.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "EXXETA AG",
  "license": "Apache v2",
  "devDependencies": {
    "@wordpress/scripts": "7.1.3",
    "node-sass": "^4.13.1"
  }
}
